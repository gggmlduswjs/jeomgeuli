<!-- 9793f539-2cf3-4fe0-af38-cb43240dc023 e2319575-130a-422e-a206-fab7acd38d4c -->
# 하드웨어 연동 구현 계획

## 목표

HARDWARE_SPEC.md에 명시된 스펙에 따라 점글이 하드웨어 시스템을 완전히 연동합니다.

## 구현 범위

### 1. 프론트엔드 수정

- `useBrailleBLE.ts`: 점글이 하드웨어 설정 추가
- `Explore.tsx`: 점글이 하드웨어 사용하도록 수정

### 2. Raspberry Pi BLE 서버 생성

- `raspberrypi/` 폴더 생성
- `raspberrypi/ble_server.py`: BLE → Serial Bridge 서버 구현
- `raspberrypi/README.md`: 설치 및 실행 가이드

### 3. Arduino 펌웨어 생성

- `arduino/` 폴더 생성
- `arduino/braille_firmware.ino`: 점자 모듈 제어 펌웨어
- `arduino/README.md`: 업로드 및 사용 가이드

### 4. 문서 업데이트

- `README.md`: 하드웨어 연동 섹션 추가

## 상세 작업

### Phase 1: 프론트엔드 설정

1. `frontend/src/hooks/useBrailleBLE.ts` 수정

- HARDWARE_CONFIGS에 `jeomgeuli` 설정 추가
- Service UUID: `12345678-1234-5678-1234-56789abcdef0`
- Characteristic UUID: `abcdabcd-1234-5678-1111-abcdefabcdef`
- namePrefix: `"Jeomgeuli"`
- 타입에 `'jeomgeuli'` 추가

2. `frontend/src/pages/Explore.tsx` 수정

- useBraillePlayback의 ble 설정을 점글이 하드웨어로 변경
- 하드코딩된 UUID 제거

### Phase 2: Raspberry Pi BLE 서버

1. `raspberrypi/` 폴더 생성
2. `raspberrypi/ble_server.py` 생성

- HARDWARE_SPEC.md의 스펙에 맞춰 구현
- BLE 서버 설정 (Device Name, UUID)
- Serial 통신 설정 (115200 baud)
- BLE → Serial Bridge 로직

3. `raspberrypi/README.md` 생성

- 설치 방법 (bluezero, pyserial)
- 실행 방법
- 문제 해결 가이드

### Phase 3: Arduino 펌웨어

1. `arduino/` 폴더 생성
2. `arduino/braille_firmware.ino` 생성

- HARDWARE_SPEC.md의 스펙에 맞춰 구현
- 핀맵: D2(DATA), D3(LATCH), D4(CLOCK)
- Serial 통신 (115200 baud)
- 점자 패턴 변환 로직 (기본 구조)
- braille.h 라이브러리 사용

3. `arduino/README.md` 생성

- 업로드 방법
- 하드웨어 연결 방법
- 문제 해결 가이드

### Phase 4: 문서 업데이트

1. `README.md` 업데이트

- 하드웨어 연동 섹션 추가
- 실행 순서 명시
- 필요한 하드웨어 목록

## 참고 파일

- `docs/HARDWARE_SPEC.md`: 모든 스펙 정의
- `frontend/src/hooks/useBrailleBLE.ts`: 현재 BLE 훅 구현
- `frontend/src/pages/Explore.tsx`: 점자 출력 사용 페이지

## 불변 규칙 (HARDWARE_SPEC.md)

- BLE Local Name: `"Jeomgeuli"` (변경 금지)
- Service UUID: `12345678-1234-5678-1234-56789abcdef0` (변경 금지)
- Characteristic UUID: `abcdabcd-1234-5678-1111-abcdefabcdef` (변경 금지)
- Arduino 핀맵: D2, D3, D4 (변경 금지)
- Serial 보드레이트: 115200 bps (변경 금지)

### To-dos

- [ ] docs/HARDWARE_SPEC.md 파일 생성 - 전체 아키텍처, 하드웨어 사양, 프로토콜 정의
- [ ] JY-SOFT 점자 모듈 상세 스펙 추가 (물리적/전기적 사양, 내부 구성)
- [ ] Raspberry Pi BLE 서버, Arduino 펌웨어, React PWA 코드 예시 추가
- [ ] 데이터 형식, 비트 구성, 통신 프로토콜 상세 명시
- [ ] 참고 자료 및 링크 추가 (JY-SOFT 블로그, 제품 링크)
- [ ] frontend/src/hooks/useBrailleBLE.ts에 jeomgeuli 하드웨어 설정 추가 (UUID, namePrefix)
- [ ] frontend/src/pages/Explore.tsx에서 점글이 하드웨어 사용하도록 수정
- [ ] raspberrypi/ 폴더 생성 및 ble_server.py 구현 (HARDWARE_SPEC.md 스펙 준수)
- [ ] raspberrypi/README.md 생성 (설치 및 실행 가이드)
- [ ] arduino/ 폴더 생성 및 braille_firmware.ino 구현 (HARDWARE_SPEC.md 스펙 준수)
- [ ] arduino/README.md 생성 (업로드 및 연결 가이드)
- [ ] README.md에 하드웨어 연동 섹션 추가 (실행 순서, 하드웨어 목록)